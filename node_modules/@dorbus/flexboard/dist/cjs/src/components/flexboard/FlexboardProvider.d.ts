import { ReactNode, FC } from 'react';
import './Flexboard.styles.css';
interface Props {
    children?: ReactNode | ReactNode[];
}
declare const FlexboardProvider: FC<Props>;
export default FlexboardProvider;
