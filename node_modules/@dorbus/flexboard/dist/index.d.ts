import React, { FC, ReactNode } from 'react';

declare enum Position {
    left = "left",
    right = "right"
}
declare enum ResizerType {
    line = "line",
    shadowline = "shadowline",
    lane = "lane",
    gutterlane = "gutterlane"
}

interface Props$2 {
    direction?: Position;
    children?: ReactNode | ReactNode[];
    width?: number;
    minWidth?: number;
    maxWidth?: number;
    draggable?: boolean;
    flexboardStyle?: React.CSSProperties;
    resizerType?: ResizerType;
    resizerStyle?: React.CSSProperties;
}
declare const Flexboard: FC<Props$2>;

interface Props$1 {
    children?: ReactNode | ReactNode[];
}
declare const FlexboardProvider: FC<Props$1>;

interface Props {
    children?: ReactNode;
}
declare const FlexboardFrame: FC<Props>;

export { Flexboard, FlexboardFrame, FlexboardProvider, Position, ResizerType };
