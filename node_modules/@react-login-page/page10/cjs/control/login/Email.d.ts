import { FC } from 'react';
import { InputProps } from 'react-login-page';
export interface EmailProps extends InputProps {
    label?: React.ReactNode;
    panel?: 'login' | 'signup';
}
export declare const Email: FC<EmailProps>;
